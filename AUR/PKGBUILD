# Maintainer: LHagfoss <you@example.com>
pkgname=termchat
pkgver=0.1.2
pkgrel=1
pkgdesc="A fast, asynchronous, bi-directional terminal chat server and client"
arch=('x86_64' 'aarch64')
url="https://github.com/LHagfoss/termchat"
license=('MIT')
depends=()
makedepends=()

source_x86_64=("termchat-x86_64-linux::https://github.com/${pkgname}/releases/download/v${pkgver}/${pkgname}-x86_64-linux")
source_aarch64=("termchat-aarch64-linux::https://github.com/${pkgname}/releases/download/v${pkgver}/${pkgname}-aarch64-linux")

sha256sums_x86_64=('SKIP')  # Update with actual checksum from release
sha256sums_aarch64=('SKIP') # Update with actual checksum from release

package() {
  install -Dm755 "${pkgname}-x86_64-linux" "${pkgdir}/usr/bin/${pkgname}"
}
